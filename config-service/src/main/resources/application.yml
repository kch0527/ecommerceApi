spring:
  application:
    name: config-service
  #profiles:
    #active: native
  cloud:
    config:
      server:
        #native:
          #search-locations: file:///C://work
        git:
          default-label: main
          uri: https://github.com/kch0527/sideProject

server:
  port: 8888
